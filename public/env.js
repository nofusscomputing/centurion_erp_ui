/**
 * UI Configuration
 * 
 * By default the settings within this file are for development.
 * When the UI is deployed (via docker) this file is updated
 * automagically by /entrypoint.sh to contain the user provided
 * configuration
 */
window.env = {
    API_URL: 'http://127.0.0.1:8002/api/v2',
  };
